import { BuscarAlumnoPipe } from './buscar-alumno.pipe';

describe('BuscarAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
