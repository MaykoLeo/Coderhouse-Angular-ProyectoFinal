import { LetraTamanioDirective } from './letra-tamanio.directive';

describe('LetraTamanioDirective', () => {
  it('should create an instance', () => {
    const directive = new LetraTamanioDirective();
    expect(directive).toBeTruthy();
  });
});
