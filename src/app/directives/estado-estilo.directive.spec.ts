import { EstadoEstiloDirective } from './estado-estilo.directive';

describe('EstadoEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
